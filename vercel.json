{
  "version": 2,
  "buildCommand": "npx vite build && cp -r migrations api/migrations && npx esbuild api/index.ts --bundle --platform=node --format=cjs --outfile=api/handler.js --external:@vercel/node --external:bcrypt --external:better-sqlite3 --external:pg --external:multer",
  "outputDirectory": "dist/public",
  "installCommand": "npm install --include=dev",
  "framework": null,
  "functions": {
    "api/handler.js": {
      "maxDuration": 30,
      "includeFiles": "node_modules/bcrypt/**,node_modules/node-pre-gyp/**,node_modules/@mapbox/**,migrations/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/cron",
      "destination": "/api/cron"
    },
    {
      "source": "/api/:path*",
      "destination": "/api/handler"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}